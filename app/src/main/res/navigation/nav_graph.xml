<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/compassDestination">

    <!--
        Phase 1: Single destination — compass only.
        Future phases MUST add destinations here and MUST NOT add hidden/disabled mode tabs.
    -->
    <activity
        android:id="@+id/compassDestination"
        android:name=".ui.CompassActivity"
        android:label="Compass" />

</navigation>
